# Changelog

## [1.13.1] - 2026-07-26

### Fixed

- 漂浮窗恢复 Windows 原生拖动，不再被屏幕边缘“吸住”；调整尺寸后继续拖动会保留当前宽高，不再跳回默认大小。
- 漂浮窗新增独立常驻异常计时：背书始终显示“未确认本人 N 秒”，自习始终显示“异常声音 N 秒”；悬停操作、动画、休息和正常状态都不会把该行隐藏。
- 多份本人声纹改为分别与当前声音匹配，任一经过录入质检的模板达到原阈值即可；不再把不同麦克风/环境模板先平均成一份总声纹，避免相互稀释导致本人误拒。
- 对高置信本人声音增加严格的 2.0 秒快速确认通道，快速阈值为 0.74，高于原 0.70 强匹配线；未达到快速门槛时仍完整回落到原 2.4 秒复核，0.55/0.70 正式阈值均未降低。
- 修复快速复核期间已经攒够 2.4 秒、但随后恰好停声时标准兜底不再启动的竞态；复核结束后会立即检查已缓存声音。

### Validation

- 仅执行后台 Node 语法、窗口源码/策略、声纹策略与实际 CAM++ 匿名 fixture 回归；没有启动 Electron、UI/CDP、候选 EXE、窗口、托盘、真实麦克风或真实声纹。

## [1.13.0] - 2026-07-22

### Changed

- 当前可见产品名、窗口、托盘、快捷方式与 1.13.0 安装/便携文件名统一为“凛冬督学局”。
- 漂浮窗除操作按钮外的状态区、计时区和动画区均可直接拖动；取消漂浮窗双击放大，只保留悬停后明确可见的“放大”按钮，避免拖动与双击手势冲突。
- “隐藏到后台”按钮在鼠标悬停或键盘聚焦时展开“完全隐藏 / 使用漂浮窗”，选择后立即进入对应方式并保存；菜单按场景位置选择展开方向，避免与主操作按钮堆叠。
- 动画播放期间的统一状态改为黄色“好好学！盯着你呢！”，不再显示容易被误解为故障的“检测暂停”，也不使用违规红色。
- 背书模式移除用户端底噪/抗噪滑块、两条白色门槛线、漂浮窗底噪条和手动重校准；用户只调 20～60 秒的未检测到本人提醒时间，学习中修改不会清空已累计的未确认时长或在途候选。
- 背书前置检测改为内部固定 8 dB 余量的自动环境适应与高召回 VAD 分段，最终仍由本地 CAM++ 声纹确认本人。若最初约 3 秒已被朗读污染，噪声基线采用保守上限，并要求适应结束后 300 ms 内恢复疑似语音候选。
- 自习模式采用 0.12 的人声专用阈值、0.20 的音乐/通用媒体阈值，并扩充电视、广播、游戏音效、爆炸、枪声、车辆、警报等媒体事件范围；键盘单独出现仍不累计。
- 自习媒体候选遇到孤立阴性分类窗时不立即清零；只有连续正常 5 秒才清除本轮累计并恢复普通状态。短暂停顿后继续播放会沿用原候选时间，避免用断续播放反复重置。
- 漂浮窗默认 320×225（本版最大尺寸），可在 224×170～320×225 间自由缩放；用户尺寸会在本机保存并于下次进入漂浮模式时恢复。
- 学习模式、两个提醒时长和用户明确选择的麦克风改由独立本机设置文件持久化；已选设备缺失时保留原 ID/标签并要求手动重选，不按同名设备或系统默认设备静默替换。
- 表扬里程碑在完全隐藏或漂浮模式下会临时恢复同一个完整主场景，字幕与源音轨播放完毕后自动返回原后台方式；普通巡查仍不会强行弹到前台。
- 违规提醒在窗口恢复前先准备遮罩，返回后台后才撤下，避免慢机短暂闪出完整控制界面；声纹正在复核时绝不触发违规，单次推理超过 5 秒或模型/服务异常会安全停止会话，不再按“本人未出声”处罚。
- 动画分片改为全局最多驻留 3 张大图：当前分片、下一分片和下一段首分片按需预取，旧图与废弃请求及时释放；加载较慢时保持上一帧，不再同时解码整套三段动画。
- 漂浮窗拖到屏幕边缘外时会立即夹回当前显示器工作区。

### Privacy

- 为避免升级后丢失或重复录入声纹，1.13.0 不迁移既有 `%APPDATA%\背书自习监督` 数据根；其中 `window-preferences.json` 仅保存 `backgroundMode` 与 `floatingWindowSize`，不保存窗口位置、检测结果、学习时长、音频、声纹或其他个人数据。
- 固定 8 dB 前置余量不持久化；旧设置中的 `reciteSensitivityDb` 会被忽略，并在下一次正常保存设置时移除。
- 声纹删除接口必须提供合法且存在的具体模板 ID；空值、路径型、非法或不存在的 ID 均拒绝，不能再被解释为“删除全部”。
- 新增 `study-preferences.json` 只保存模式、20～60 秒、3～15 秒和选定麦克风 ID/标签，不含音频、声纹、检测结果、学习记录或计时；文件被 Git 与发布包排除。

### Validation

- 本轮只执行不触碰用户桌面的后台纯 Node 语法、策略、源码结构、模型与隐私门禁；没有启动 Electron、UI/CDP、候选 EXE、窗口、托盘、真实麦克风或真实声纹，不能描述为 UI 实测通过。
- 41 个 JavaScript 文件语法检查、策略/声纹/DPAPI/真实键盘与媒体混合样本、三分片动画缓存和刁难用户式纯 Node 回归均通过；未启动 Electron、UI/CDP、候选 EXE、窗口、托盘、真实麦克风或真实声纹。
- 1.13.0 安装版与便携版已在隔离候选目录构建并静态验包：22 段动画、22 份源音轨、两套模型和 19 个核心源码完整，包内禁止项为 0；两个 EXE 均未做商业代码签名。

## [1.12.0] - 2026-07-22

### Added

- 新增可持久化的后台显示方式：可选择完全隐藏，或把原主窗口切换为 320×225 的置顶漂浮窗。
- 漂浮窗常态只显示统一检测结果和 16:9 教官动画；鼠标移入时显示本次有效学习时长，以及“隐藏”“放大”两个操作。
- 新增纯 Node 的窗口模式策略、偏好文件、IPC 边界和源码结构门禁，不启动 Electron、窗口、麦克风或用户声纹即可运行。

### Changed

- 漂浮模式复用原 `BrowserWindow`、`webContents`、Canvas、动画播放器和麦克风流，不创建另一套检测或动画窗口。
- 非致命提醒改为由主进程签发提醒编号并记录来源模式；完整场景、漂浮窗、完全隐藏分别在提醒结束后回到原状态，过期或额外字段的结束请求不会改变窗口。
- 休息提示出现时，漂浮窗自动移到提示窗上方；空间不足时移到左下角，提示关闭后回到用户原来的漂浮位置。
- 完整场景、漂浮窗和提醒之间切换时，主进程先隐藏同一窗口，等待渲染布局连续两帧就绪后再一次显示，避免大小与布局错帧造成黑屏或跳变。
- 标题栏关闭键与 `Alt+F4` 统一遵循当前后台选择；窗口模式切换和后台偏好写入均串行执行，连续点击不会让旧操作覆盖新选择。
- 提醒播放期间拒绝最小化、最大化和普通恢复；休息券提示会暂时隐藏，提醒结束后再按原状态恢复，避免小窗遮住提醒。
- 双屏环境先锁定漂浮窗所在屏幕再展开完整场景；重复点击当前场景或漂浮模式走幂等路径，不再无意义地隐藏重显。
- 渲染布局未能在时限内确认时保持主窗口隐藏，不显示尺寸与布局不匹配的残缺界面。

### Privacy

- 后台偏好只在 `%APPDATA%\背书自习监督\window-preferences.json` 保存 `hidden` 或 `floating`，不包含音频、声纹、检测结果、计时或个人信息。

## [1.11.1] - 2026-07-21

### Changed

- 自习模式完全不创建或使用 `AdaptiveVad`，取消 3 秒环境底噪校准和音量/sensitivity 门槛；麦克风打开后直接进入本地 CED Mini 分类，首个有效分类约需 2 秒，之后约每 1 秒更新。
- 自习界面隐藏白色门槛线、抗噪幅度滑块和“重新校准环境底噪”，音量条只展示原始 RMS 活动强度；用户只调 3～15 秒媒体持续时间。
- 自习的媒体候选要求连续分类；任一阴性窗口立即清空候选，连续阳性仍抵扣约 1 秒滚动窗口重叠。
- 休息结束并播放有声开场后，自习直接恢复分类并重新形成首窗；背书仍按原流程重新校准约 3 秒。

### Unchanged

- 键盘单独识别仍不累计违规；键盘与媒体双证据仍可累计。
- 背书模式的环境底噪校准、抗噪幅度、可拖动门槛与声纹验证保持不变。

### Validation

- 仅运行后台纯 Node 门禁；真实键盘、键盘与音乐/人声混合、合成稳态风扇及声纹服务回归均通过，临时音频 fixture 已删除。
- 安装版与便携版已在隔离目录构建并解包核对；按用户要求没有启动 Electron、UI/CDP、候选 EXE、真实麦克风或用户声纹。

## [1.11.0] - 2026-07-21

### Added

- 自习模式新增本地 CED Mini 音频事件识别：以最近约 2 秒滚动窗口、约 1 秒更新频率判断人声、音乐、电视/广播、游戏等媒体声音。
- 连续分类先抵扣约 1 秒窗口重叠；有限的 2 秒媒体片段不会因三个重叠窗口而被累计成 3 秒。学习中调整门槛或持续时间会立即作废旧缓冲与在途结果，再按新设置暖机。
- 新增键盘与媒体的双证据策略：单独键盘标签不累计违规；响键盘下若同时出现两项媒体证据，媒体仍可继续累计。
- 新增纯 Node 的 `audio-event-policy-test.cjs` 与 `audio-event-model-smoke.cjs`，无需启动 Electron、窗口、麦克风或用户声纹。

### Changed

- 自习模式请求原始麦克风音频时主动关闭浏览器层回声消除、降噪和自动增益，避免电脑扬声器媒体声被优先消除；若系统驱动不遵守请求，界面会提示这一限制。
- 自习开始或待命测试的首次有效分类约需 2 秒，之后约每秒更新判断；提醒仍按用户设置的 3～15 秒媒体证据累计。

### Privacy

- CED Mini 推理、标签与概率只在本机内存中使用；原始 PCM、分类标签和概率不写盘、不上传。
- 本轮自动验证全部使用后台纯 Node 测试，不启动 UI、不访问真实麦克风或 `%APPDATA%\背书自习监督\speaker-profile.dat`。

### Limitations

- 麦克风听不到的静音视频或耳机声音无法检测；极端键盘掩蔽仍可能漏判，正常课程、旁人说话或其他合法持续音频也可能被归为媒体证据。

## [1.10.1] - 2026-07-21

### Fixed

- 声纹档案改为使用当前 Windows 用户的 DPAPI 本地加密，修复此前退出后无法再次载入、导致每次启动都要求重新录入的问题。
- 保持每次运行的浏览器会话缓存清理；声纹特征仍只保存在本机，不上传、不保存原始录音。
- 旧版无法载入的声纹不再伪装为“尚未录入”，界面会明确提示需要最后重新录入一次。

## [1.10.0] - 2026-07-20

### Added

- 麦克风选择与刷新：所选设备同时用于待命测试、正式学习和声纹录入；设备断开时不会静默回退到其他麦克风。
- 同一用户最多可保存 5 份本地声纹模板，适合不同麦克风或环境；运行时匹配任一模板即可。

### Changed

- 声纹档案升级为 schema 3。旧 schema 2 单份档案会以“原有声纹”载入，新增或删除模板后才原子写入新档案；原始录音仍不保存、不上传。

本文件只记录已发布行为，不替代用户说明、架构或测试文档。

## [1.9.0] - 2026-07-15

### Changed

- 应用、窗口、托盘、桌面和开始菜单名称统一为“背书自习监督”。
- 交付方式改为可选择安装目录的 Windows NSIS 安装包；不再生成新的便携程序副本。
- 主场景和休息提示改用禁用 HTTP 缓存的共享内存会话，同时禁用 V8 代码缓存和拼写词典缓存。

### Privacy

- 正常退出后清理隔离的默认会话临时目录，不持久化 `SessionData`、`Code Cache`、`GPUCache` 等浏览器运行缓存；唯一保留的麦克风衍生数据仍是用户主动录入的加密声纹。
- 新安装版不读取、复制或迁移旧便携版的声纹档案，首次使用需由用户主动重新录入。

## [1.8.0] - 2026-07-15

### Added

- 检测面板新增待命测试：开始学习前即可用正式麦克风、底噪校准、VAD、声纹和自习判定链路试调提醒时间与抗噪幅度。
- 主界面常驻音量条与详细音量条的白色门槛线支持鼠标、触屏和键盘直接粗调，并同步隐藏滑块、持久化设置及运行中的检测器。
- 应用标题下、README 开头和作者文档显示“原作：叛逆蓝牙 · 二创：眼泪斷了线”。

### Changed

- 测试达到当前阈值时只显示“按当前设置将触发提醒”，不开始会话、不计时、不播放场景或音轨，也不增加提醒等级。
- GitHub 首次提交排除用户数据、构建产物和真人测试语音；公开仓库纳入完整动画、对应源音轨与应用图标，正式 EXE 仅作为 GitHub Release 附件提供。

### Fixed

- 开始学习、切换模式、进入声纹录入、最小化/隐藏窗口、退出或麦克风断开时会停止待命测试并释放音频资源，避免重复占用麦克风。
- 开始学习、动画预览、声纹录入和声纹删除增加待命态互斥与等待后复核，修复快速连点可能造成的场景并发、双麦克风流或启动中修改声纹。
- 动画预览结束后统一恢复模式、开始和声纹按钮，避免控件保持禁用。
- 自习预检中修改持续时间或出声门槛会清除旧阈值留下的锁定结果，按新设置重新累计。
- 署名和测试控件在 960×540 最小窗口及窄屏下保持可见、可滚动且不与现有按钮堆叠。

## [1.7.1] - 2026-07-15

### Added

- 无框自定义标题栏：支持拖动、最小化、最大化/还原、双击切换和关闭即隐藏。

### Changed

- 主窗口允许自由缩放，场景 Canvas 始终按 16:9 等比例适配；安全最小窗口尺寸为 960×540。

### Fixed

- 修复极小窗口下模式按钮、学习按钮与标题栏控件互相遮挡的问题。
- IPC 现在同时绑定具体窗口、主 frame 与精确页面 URL，并阻止新窗口和非预期导航。
- 主页面和休息提示页拆分为独立 preload，避免向提示页暴露声纹、窗口或退出能力。
- 提醒结束参数改为严格布尔 schema，窗口销毁期间读取状态不再访问失效对象。

## [1.7.0] - 2026-07-15

### Added

- 背书与自习双模式；主学习面板始终显示实时出声状态和音量。
- 背书 20～60 秒、自习持续人声 3～15 秒的可调提醒阈值。
- 有效学习时钟、可累计的两分钟休息券，以及右下角独立休息提示窗。
- 背书每 45 分钟、自习每 60 分钟的里程碑表扬。

### Changed

- 产品名、窗口标题和便携程序统一为“凛冬督学局”。
- 背书每 20 分钟获得休息券，自习每 45 分钟获得休息券；休息时间不计入有效学习时长。
- 休息结束后主窗口回到前台，播放一次有源音轨的开场并重新校准麦克风。
- 自习违规只累计连续原始人声证据，忽略 VAD 尾音和键盘等短促瞬态。

### Fixed

- 动画解码、窗口 IPC 或停止竞态失败时统一安全结束，防止卡在开始、恢复、提醒或结束状态，并确保麦克风释放。

## [1.6.0] - 2026-07-15

### Added

- 本地麦克风声纹录入：一次 24 秒采集，8 个候选保留最一致的 6 个。
- 整点表扬序列及右下方不遮挡教官的累计小时字幕。
- 录入稳态噪声动态检查、本人/他人多次复核和验证宽限。
- 单窗口后台隐藏与同窗口违规恢复。

### Changed

- 静默提醒阈值为 20 秒，提醒按训诫、举枪、最终处置三级升级。
- 普通巡查、观察和路过全部静音；仅开场、结束、违规和整点表扬播放对应源音轨。
- 目标展示固定为 1920×1080 等比例布局，动画切换保留末帧以避免黑屏。
- 产品名统一为 `beishu`，便携 EXE 位于 `dist/beishu.exe`。

### Removed

- 摄像头和手机检测路径。
- 音频文件导入声纹功能。
- 新建提醒窗口和摄像头小窗。

### Breaking

- 声纹档案升级为 schema 2；1.5.x 及更早版本的档案需要重新录入。
